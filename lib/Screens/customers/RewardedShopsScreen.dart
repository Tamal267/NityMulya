import 'package:flutter/material.dart';

class RewardedShopsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Loyalty Rewards")),
      body: Center(child: Text("Your loyalty points & rewarded shops")),
    );
  }
}
